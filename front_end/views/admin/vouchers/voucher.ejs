<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD Mã Giảm Giá</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h2 class="text-center">Tạo Mã Giảm Giá</h2>
        <form>
            <div class="mb-3">
                <label class="form-label">Tên mã giảm giá*</label>
                <input type="text" class="form-control" placeholder="Nhập mã giảm giá" required>
            </div>
            
            <div class="mb-3">
                <label class="form-label">Loại mã giảm giá*</label>
                <div>
                    <input type="radio" name="discountType" value="percent" checked> Giảm giá theo %
                    <input type="radio" name="discountType" value="amount" class="ms-3"> Giảm giá theo số tiền
                </div>
            </div>
            
            <div class="mb-3" id="percentSection">
                <label class="form-label">Mức giảm giá (%)</label>
                <select class="form-control">
                    <option value="10">10%</option>
                    <option value="15">15%</option>
                </select>
            </div>
            
            <div class="mb-3" id="amountSection" style="display: none;">
                <label class="form-label">Mức giảm giá (VNĐ)</label>
                <select class="form-control">
                    <option value="15000">15,000đ</option>
                    <option value="20000">20,000đ</option>
                </select>
            </div>
            
            <div class="mb-3">
                <label class="form-label">Thời gian bắt đầu*</label>
                <input type="date" class="form-control" required>
            </div>
            
            <div class="mb-3">
                <label class="form-label">Thời gian kết thúc*</label>
                <input type="date" class="form-control" required>
            </div>
            
            <div class="mb-3">
                <label class="form-label">Ghi chú</label>
                <textarea class="form-control" rows="3" placeholder="Nhập ghi chú (tùy chọn)"></textarea>
            </div>
            
            <button type="submit" class="btn btn-primary">Tạo mã giảm giá</button>
        </form>
    </div>

    <script>
        document.querySelectorAll("input[name='discountType']").forEach(input => {
            input.addEventListener("change", function() {
                if (this.value === "percent") {
                    document.getElementById("percentSection").style.display = "block";
                    document.getElementById("amountSection").style.display = "none";
                } else {
                    document.getElementById("percentSection").style.display = "none";
                    document.getElementById("amountSection").style.display = "block";
                }
            });
        });
    </script>
</body>
</html>
